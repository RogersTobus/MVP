# 프로젝트 작업 규칙

- 웹 검색이나 크롤링은 반드시 설정된 Chrome 원격 디버깅 세션을 통해 진행한다. CAPTCHA, 보안 확인, 접근 제한은 우회하지 않는다.
- 이 작업공간을 변경한 뒤에는 관련 검사를 실행하고, 비밀정보와 임시 파일을 제외한 코드·SQLite 데이터·생성 이미지·사용자 레퍼런스를 커밋한 뒤 `origin`의 현재 브랜치로 푸시한다.
- `.env`, 로그인 세션, 쿠키, 토큰, 자동 수집한 타 사이트 원본 이미지(`public/references/web-*`)는 GitHub에 올리지 않는다.
- 자동 수집한 웹 이미지는 생성 결과의 시각 참고용으로만 사용하며 그대로 게시하거나 복제하지 않는다.
